{% extends 'base.html.twig' %}

{% block title %}Mes commandes - La Boutique Antillaise{% endblock %}

{% block content %}
    <h1>Mes commandes</h1>
    <p>Vous pouvez voir et gérer ici toutes vos commandes.</p>
    {% if orders|length == 0 %}
        <p class="text-center">Vous n'avez pas encore passé commande sur La Boutique Antillaise. Passez faire un tour sur <a href="{{ path('products') }}">notre catalogue</a>.</p>
    {% else %}

        <div class="table-responsive">
            <table class="table table-hover table-striped mt-4">

                <thead>
                <tr>
                    <th scope="col">#</th>
                    <th scope="col">Référence</th>
                    <th scope="col">Passée le</th>
                    <th scope="col">Produits</th>
                    <th scope="col">Total</th>
                    <th scope="col"></th>
                </tr>
                </thead>

                <tbody class="table-striped">
                {% for key, order in orders %}
                    <tr>
                        <th scope="row">{{ key + 1 }}</th>
                        <td><span class="badge badge-secondary">{{ order.reference }}</span></td>
                        <td>{{ order.createdAt|date('d-m-Y') }}</td>
                        <td>{{ order.orderDetails|length }}</td>
                        <td>{{ ((order.carrierPrice + order.getTotal) / 100)|number_format(2, ',', ' ') }} €</td>
                        <td class="text-right"><a href="{{ path('account_order_show', {reference : order.reference}) }}">Afficher</a></td>
                    </tr>
                {% endfor %}
                </tbody>

            </table>
        </div>
    {% endif %}
    {# TODO: Ajouter mes adresses #}
    <hr>
    <a href="{{ path('account') }}">Retour</a><br>
{% endblock %}
