{% extends 'base.html.twig' %}

{% block title %}Mes adresses - La Boutique Antillaise{% endblock %}

{% block content %}
    <a href="{{ path('account_address_add') }}" class="btn btn-info btn-sm float-right">Ajouter une adresse</a>
    <h1>Mes adresses</h1>
    <p>Vous pouvez gérer ici toutes vos adresses.</p>
    <hr>
    {% if app.user.addresses|length == 0 %}
        <p class="text-center">Vous n'avez pas d'adresse dans votre compte client. Pour en ajouter, veuillez <a href="{{ path('account_address_add') }}">cliquer ici</a>.</p>
    {% else %}
        <div class="row">
            {% for address in app.user.addresses %}
            <div class="col-sm-4">
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title">{{ address.name }}</h5>
                        <p class="card-text">
                            {{ address.firstname }} {{ address.lastname }}<br>
                            {{ address.address }}<br>
                            {{ address.postal }}, {{ address.city }}<br>
                            {{ address.country }}</p>
                        <a href="{{ path('account_address_edit' , {id: address.id}) }}" class="">Modifier</a> |
                        <a href="{{ path('account_address_delete', {id : address.id}) }}" class="">Supprimer</a>
                    </div>
                </div>
            </div>
            {% endfor %}
        </div>
    {% endif %}
    {# TODO: Ajouter mes adresses #}
    <hr>
    <a href="{{ path('account') }}">Retour</a><br>
{% endblock %}
