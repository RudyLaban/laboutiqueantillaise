{% extends 'base.html.twig' %}

{% block title %}Valider ma commande - La boutique antillaise{% endblock %}

{% block content %}
    <h2>Validation de commande</h2>
    <p>Choisissez vos préférences avant de valider votre commande.</p>
    <hr>
        <div class="row">
            <div class="col-md-6">
                {% set formHtml %}
                    {{ form(form) }}
                {% endset %}
                {{ formHtml|replace({'[br]' : '<br/>'})|raw }}
            </div>
            <div class="col-md-6">
                <legend class="col-form-label">Récapitulatif de ma commande</legend>
                <div class="order-summary">
                    {% for key, product in cart %}
                        <div class="row">
                            <div class="col-2">
                                <img src="/uploads/{{ product.product.illustration }}" alt="{{ product.product.name }}" height="60px">
                            </div>
                            <div class="col-8 my-auto">
                                {{ product.product.name }}<br>
                                <small>{{ product.product.subtitle }}</small>
                            </div>
                            <div class="col-2 my-auto">
                                x {{ product.quantity }}
                            </div>
                        </div>
                    {% endfor %}
                </div>
            </div>
    </div>
{% endblock %}
